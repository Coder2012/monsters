import { NavList } from '../Navigation';

export const Home = () => {
  return (
    <section>
      <header className="App-header">
        <h1>Monsters Rewards!</h1>
      </header>
      <NavList />
    </section>
  );
};
